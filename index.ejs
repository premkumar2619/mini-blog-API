<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All post</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora post</h1>
    
    <%for(post of posts){%>
        <div id="post">
            <p>@<%= post.username %></p>
            <p><%= post.content %></p>
            <a href="http://localhost:8080/posts/<%= post.id %>">See post in details</a>
            <a href="http://localhost:8080/posts/<%= post.id %>/edit">Edit</a>
            <form action="http://localhost:8080/posts/<%= post.id %>/delete?_method=DELETE" method="post">
                <button>Delete Post</button>
            </form>
        </div>
        <%}%>
        <a href="http://localhost:8080/posts/new">Create new post</a>
</body>
</html>
